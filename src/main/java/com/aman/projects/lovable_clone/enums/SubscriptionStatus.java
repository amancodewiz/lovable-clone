package com.aman.projects.lovable_clone.enums;

//An enum is a fixed set of constants.
//user defined data type with constraints.
//Use it when values are limited and predefined.->SubscriptionStatus can only have these values. Nothing else.
//strict control over values

public enum SubscriptionStatus {
    ACTIVE,TRIALING,CANCELED,PAST_DUE,INCOMPLETE
}
